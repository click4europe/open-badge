{{ drupal_entity('block_content', 6) }}
<div class="max-w-6xl mx-auto px-4 sm:px-6 space-y-10">
    {{ drupal_block('system_messages_block', wrapper=false) }} 
    {% block content %}
         <main class="mx-5 mt-60 min-h-80 text-2xl">
          {{ page.content }}
         </main>
    {% endblock %}
</div>
{{ drupal_entity('block_content', 5) }}