{#
/**
 * @file
 * Theme override for Basic Page nodes.
 * 
 * Renders the BasicPageRender block which handles the page layout.
 */
#}

{# Render the BasicPageRender block for this node #}
{{ drupal_block('basic_page_render') }}
